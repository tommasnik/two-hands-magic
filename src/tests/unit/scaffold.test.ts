import { describe, it, expect } from 'vitest'

// Placeholder — remove once real game logic tests exist
describe('scaffold', () => {
  it('test infrastructure is wired up', () => {
    expect(1 + 1).toBe(2)
  })
})
